//文件Plane.java
package CalTime.vehicle;
import CalTime.vehicle.all.Common;  //导入Common接口
public class Plane implements Common {
    public double runTimer(double a, double b, double c) {
        return (a + b + c);
    }
}